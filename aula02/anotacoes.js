/*
 Estrutura léxica 
 
 - Case sensitive: Existe diferença entre letras maiusculas e letras minusculas. Exemplo: Animal # animal # ANIMAL
 
 - Literal: (Pesquisar sobre) Já existe na linguagem e não muda 

 - Identificadores: Nome de var  e funções 

 - Formas de usar o if, else e if else

 Operadores lógicos 

- AND (&&) - expr1 && expr2 
Retorna expr1 caso possa ser convertido para falso; senão, retorna expr2. Assim, quando utilizado com valores 
booleanos, && retorna verdadeiro caso ambos operandos sejam verdadeiros; caso contrário, retorna falso.

- OU (||) expr1 || expr2 
Retorna expr1 caso possa ser convertido para verdadeiro; senão, retorna expr2. Assim, quando utilizado com valores 
booleanos, || retorna verdadeiro caso ambos os operandos sejam verdadeiro; se ambos forem falsos, retorna falso.

- NOT (!) !expr 
Retorna falso caso o único operando possa ser convertido para verdadeiro; senão, retorna verdadeiro.
  
 */